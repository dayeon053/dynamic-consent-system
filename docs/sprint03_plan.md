# Sprint 03 계획 (2026-07-21 ~ 2026-07-27)

## 반영 범위
- PR 리뷰 코멘트 반영: `UserConsentCheck`의 setter 누락으로 인한 컴파일 문제를 해결하고, 실제 사용 경로와 일치하도록 엔티티 상태를 확인한다.
- 스프린트 계획에 빠졌던 항목을 추가해 백엔드/위험도 모듈 공통 작업 범위를 명확히 정리한다.
- 위험도 파이프라인, API 명세, 테스트/문서화 항목을 함께 관리한다.

## 노가현 (백엔드)
- [ ] 2-4 DB 날짜별 누적 저장: `RiskScore`에 매일 1회 `(user_id, company_id, scored_at)` 기준 새 row insert(append-only) 방식을 적용한다. 동일 날짜 중복 방지와 `GET /users/{user_id}/companies/{company_id}/risk-history` 날짜별 조회 확인까지 포함한다.
- [ ] API 명세 최신화: 실제 구현된 응답 필드와 API 문서의 일치 여부를 점검하고 수정한다.
- [ ] 크롤링/위험도 파이프라인 실행 연결: `RiskPipelineService`가 실제로 호출되지 않는 상태와 크롤러 중복 구현(`CompanyPolicyCrawlService`/구버전 구현) 정리·통합 계획을 수립하고 반영한다.
- [ ] PR 리뷰 반영: `UserConsentCheck` setter 누락 문제를 해결하고, 빌드/컴파일 검증 결과를 공유한다.

## 황다연 (위험도)
- [ ] 2-3 등급 분류 경계값 테스트: 5단계 경계값(3.0~6.9, 7.0~13.9, 14.0~23.9, 24.0~35.9, 36.0~45.5) 경계 자체 포함 테스트 케이스를 작성한다.
- [ ] 위험도 모듈 리팩터링: Repository 통합 시 타입 불일치·null 처리 점검 및 README에 함수 시그니처 문서화를 반영한다.
- [ ] 개인 맞춤 위험도 서버 구현 여부 결정: 2-2/2-5 관련 로직이 현재 프론트(Kotlin)에만 있는 상태이므로 서버 구현 필요 여부와 담당자를 확정한다.
- [ ] common-model 모듈 단위 테스트 보강: 위험도 산출 공식(2-1), 등급 분류(2-3), LLM 파싱(1-4) 등 핵심 계산 로직 회귀 테스트를 추가한다.

## 공통/운영
- [ ] PR/브랜치 리뷰 대응 이슈를 정리하고, 머지 우선순위와 충돌 가능 영역을 문서화한다.
- [ ] 위험도/LLM 파이프라인의 실제 실행 검증 결과를 기록하고, 다음 스프린트로 이어질 작업을 정리한다.
