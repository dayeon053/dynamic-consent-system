# 한글 리터럴이 포함된 소스를 빌드하는 JVM(Gradle 데몬 포함)이 OS 기본 인코딩(Windows의
# x-windows-949 등)이 아닌 UTF-8을 기본값으로 쓰도록 강제한다. build.gradle의
# JavaCompile.options.encoding='UTF-8'과는 별개로, Gradle 자체 및 컴파일러가 파일을
# 읽어들이는 시점의 플랫폼 기본 인코딩도 맞춰줘야 완전히 안전하다.
org.gradle.jvmargs=-Dfile.encoding=UTF-8
